export interface ICardItem {
  // name?: string;
  // countDown?: string;
  imageProfile?: string;
  imageContent: string;
  price?: string;
  currency?: string;
  stock?: string;
  // numOfLike?: number;
  title?: string;
  // verified?: boolean;
}
