import { AvatarProps, IconProps, BoxProps } from '@chakra-ui/react';

export interface ICircularProfile {
  centerVerif?: boolean;
  verified: boolean;
  photo: string;
  name: string;
  bordered?: boolean;
  customimg?: AvatarProps;
  customverified?: IconProps;
  customBoxContainerbecouceserrothefuckingshit?: BoxProps;
}
